# 养老AI助手 - 老人端技术PRD

> 版本：v1.0
> 整理时间：2026-04-25
> 状态：功能完成，待测试验证

---

## 一、用户画像

- **主要用户**：独居老人（65岁以上），子女不在身边
- **使用场景**：日常对话、跌倒检测紧急救援、用药提醒
- **操作能力**：可能不熟悉手机/电脑操作，语音交互为主
- **痛点**：孤独感、跌倒无人知、忘记吃药

---

## 二、UI/UX设计

### 2.1 页面结构

```
养老AI助手（Tkinter）
├── 对话页（默认）
│   ├── AI名称 + 连接状态
│   ├── 聊天记录区
│   ├── 文字输入框 + 发送按钮
│   └── 【按住说话】【联系子女】【紧急呼救】按钮
│
├── 事件页
│   ├── 事件列表（姿态变化、跌倒、SOS、用药等）
│   └── 刷新按钮
│
├── 用药页
│   ├── 添加用药提醒表单
│   └── 提醒列表
│
└── 设置页
    ├── AI名称设置
    ├── 个人信息（姓名、年龄、病史、紧急联系人）
    └── 系统状态
```

### 2.2 关键UI元素

| 元素 | 样式 | 说明 |
|------|------|------|
| 按住说话按钮 | 粉红色 #f38ba8 | 语音输入 |
| 联系子女按钮 | 蓝色 #89b4fa | 触发SOS流程 |
| 紧急呼救按钮 | 红色 #f00000 | 紧急情况 |
| AI名称标签 | 橙色 #fab387 | 显示当前AI称呼 |

### 2.3 事件列表显示

| 姿态 | 显示 |
|------|------|
| standing | 🧍【站立】 |
| sitting | 🧍【坐下】 |
| walking | 🚶【行走】 |
| lying_down | 😴【躺着】 |
| falling | 🤕【跌倒】 |
| unknown | ❓【未知】 |

---

## 三、语音交互

### 3.1 语音唤醒 + 对话流程

```
日常待机状态（屏幕可亮但AI不响应）
    ↑
    │ 用户连续呼唤AI名字两遍（如"小管家小管家"）
    │
    ↓ 唤醒成功
AI立即回复"我在"（TTS语音 + 文字显示）
    ↓
进入对话状态（类似按下"按住说话"按钮）
    ↓
用户说话 → Whisper STT → LLM处理
    ↓
AI回复 = 文字 + TTS语音同步输出（同时展示和播放）
    ↓
对话结束（如10秒无活动）→ 返回待机状态
```

### 3.2 唤醒词检测

| 参数 | 值 | 说明 |
|------|-----|------|
| 唤醒词 | AI名字 × 2 | 如"小管家小管家" |
| 检测方式 | Whisper STT | 实时监听并检测唤醒词 |
| 响应延迟 | < 500ms | 唤醒后立即回复"我在" |
| 待机模式 | 屏幕常亮，AI不主动响应 | 降低功耗 |

### 3.3 AI回复：文字+语音同步

所有AI回复必须**同时**输出：
- **文字**：显示在聊天区域
- **语音**：espeak TTS合成并立即播放

两者同步触发，用户既能"看到"也能"听到"。

### 3.4 AI自动填写

对话中AI自动识别意图并操作：

| 用户说 | AI自动 |
|--------|--------|
| "提醒我每天8点吃降压药" | 创建用药提醒 |
| "我叫张三" | 更新姓名 |
| "我68岁" | 更新年龄 |
| "提醒我下午2点吃消炎药" | 创建14:00用药提醒 |

---

## 四、跌倒检测算法

### 4.1 检测逻辑

使用 MediaPipe Pose 分析躯干角度和头部高度：

```python
# 关键阈值
FALL_ANGLE_THRESHOLD = 45.0  # 躯干与垂直方向夹角 > 45° 视为跌倒
HEIGHT_DROP_RATIO = 0.35     # 高度下降 > 35% 视为跌倒
CONFIDENCE_THRESHOLD = 0.6    # 姿态检测置信度 > 0.6 才采纳
```

### 4.2 跌倒判定条件

同时满足以下条件判定为跌倒：
1. 躯干角度 > 45°（身体倾斜）
2. 头部高度下降 > 35%（相对于站立基准）
3. 持续时间 > 0.5秒（排除短暂弯腰）

### 4.3 姿态分类

| 条件 | 姿态 |
|------|------|
| 躯干角度 > 60° | 【躺着】 |
| 躯干角度 > 30° 且膝盖与臀部高度接近 | 【坐着】 |
| 躯干角度 > 30° 且不符合坐着条件 | 【跌倒】 |
| 脚踝高度周期性变化 > 0.08 | 【行走】 |
| 脚踝低于臀部 > 0.15 | 【站立】 |

---

## 五、跌倒关怀流程

### 5.1 完整流程

```
检测到跌倒（MediaPipe Pose 确认）
    ↓
记录事件 + 轨迹（最近30秒姿态数据）
    ↓
立即触发语音唤醒（如果尚未唤醒）
"我在"（2秒内必须发出）
    ↓
第1次询问（唤醒后2秒内立即发起）
"您没事吧？需要帮助吗？请说'没事'或'需要帮助'。"
    ↓ （等待20秒）
无响应 → 第2次询问
"我有点担心您，需要我联系您的子女吗？请说'好的'联系，或'没事'继续。"
    ↓ （等待20秒）
无响应 → 第3次询问
"最后一次询问，您真的没事吗？我要联系您的子女了，请说'好的'确认。"
    ↓ （等待20秒）
无响应 → 触发SOS：Webhook通知子女 + TTS播报"已联系您的子女"
```

### 5.2 跌倒关怀关键时序要求

| 阶段 | 时限 | 说明 |
|------|------|------|
| 跌倒确认 → 语音唤醒"我在" | ≤ 2秒 | 必须立即响应 |
| 唤醒"我在" → 第1次询问发出 | ≤ 2秒 | 无缝衔接 |
| 第N次询问 → 第N+1次询问 | 20秒 | 等待用户响应 |
| 无响应 → SOS触发 | 60秒 | 3次询问均无响应 |

### 5.3 用户响应关键词

| 关键词 | 含义 | 处理 |
|--------|------|------|
| 没事、我没事、好的、没问题、OK | 用户确认无恙 | 记录并继续监测 |
| 需要帮助、救命、帮帮我、打120 | 用户需要帮助 | 立即联系子女 |
| cancel | 用户取消 | 取消当前关怀流程 |

### 5.4 关怀流程状态

| 状态 | 说明 |
|------|------|
| IDLE | 无活动 |
| CHECKING_IN | 正在询问老人（1-3次） |
| ESCALATING | 正在联系子女 |
| RESOLVED | 已解决（用户确认OK或已取消） |

---

## 六、运动轨迹记录

### 6.1 数据结构

```python
# 滚动缓冲区：最近5分钟（300条）
MAX_TRAJECTORY_BUFFER = 300

{
    "timestamp": 1745337580.123,  # Unix时间戳
    "pose": "standing"            # 姿态状态
}
```

### 6.2 轨迹用途

- 跌倒事件回溯：跌倒发生前后各30秒的姿态序列
- 事件日志存储：跌倒事件包含 `trajectory` 字段
- 查询API：`GET /pose/trajectory?seconds=30`

---

## 七、用药提醒

### 7.1 创建方式

| 方式 | 操作 |
|------|------|
| 语音创建 | 对AI说"提醒我每天8点吃降压药" |
| UI创建 | 用药页 → 输入药品名 + 时间 → 添加 |

### 7.2 触发流程

```
定时任务触发（APScheduler）
    ↓
TTS语音播报："王大爷您好，您该吃降压药了。"
    ↓
记录日志（triggered / confirmed / skipped）
```

---

## 八、技术实现

### 8.1 目录结构

```
voice-ai-device/
├── app/
│   ├── main.py              # FastAPI后端
│   ├── ollama_client.py     # LLM客户端
│   ├── stt.py               # Whisper STT
│   ├── tts.py               # espeak TTS
│   ├── pose_monitor.py      # 姿态监测 + 轨迹记录
│   ├── fall_detection.py     # 跌倒检测
│   ├── fall_escalation.py    # 跌倒关怀流程
│   ├── medication_reminder.py # 用药提醒
│   ├── care_reminder.py      # 主动关怀
│   ├── notification.py       # Webhook通知
│   ├── event_log.py         # 事件日志
│   ├── user_profile.py       # 用户资料
│   ├── ai_name.py           # AI称呼
│   ├── video_capture.py      # 摄像头捕获
│   ├── voice_wakeup.py       # 语音唤醒检测
│   ├── ui/
│   │   └── tkinter_app.py    # Tkinter UI
│   └── models.py             # 数据模型
├── requirements.txt
└── run.py
```

### 8.2 依赖

| 组件 | 版本 | 用途 |
|------|------|------|
| Python | 3.12 | 运行环境 |
| Ollama | 最新 | LLM推理（gemma4:e4b） |
| openai-whisper | 最新 | 语音识别（STT + 唤醒词检测） |
| espeak | 最新 | TTS语音合成 |
| MediaPipe | 最新 | 姿态检测 |
| FastAPI | 最新 | Web框架 |
| APScheduler | 最新 | 定时任务 |

---

## 九、验收标准

| 测试项 | 通过标准 |
|--------|---------|
| 语音唤醒 | 连续呼唤AI名字两遍，2秒内回复"我在" |
| AI回复同步 | 文字+语音同时输出，用户能看到也能听到 |
| 语音对话 | 唤醒后说话，AI响应<3s，同步文字+语音 |
| 跌倒检测 | 模拟跌倒动作，5次中至少4次检测到 |
| 关怀时序 | 跌倒确认到"我在"≤2秒，"我在"到第1次询问≤2秒 |
| 关怀流程 | 检测到跌倒后，AI询问3次×20秒，无响应触发SOS |
| 轨迹记录 | 跌倒事件包含最近30秒姿态数据 |
| AI自动创建提醒 | 说"提醒我每天8点吃降压药"，定时触发 |
| 用户响应 | 说"没事"则取消SOS，说"需要帮助"则立即联系子女 |
